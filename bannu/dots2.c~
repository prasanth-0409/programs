#include <stdio.h>
int
main()
{
	int n,line,i;
	printf("Enter the value of n \n");
	scanf("%d",&n);
	line=1;
	while(n)
	{
	    for(i=0;i<n;i++)
	    {
	        printf(" ");
	    }
            for(i=0;i<line;i++)
	    {
	        printf("* ");
            }
        ++line;
        --n;
	}
}	
